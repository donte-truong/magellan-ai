export default function Page() {
  return <main style={{ maxWidth: 800, margin: '60px auto', fontFamily: 'system-ui' }}><h1>Supply-chain research backend</h1><p>The local research API is available under <code>/v1</code>. API requests require the configured bearer token.</p><p>Run <code>npm run demo</code> to generate an offline evidence graph and HTML inspector. See the backend README for live research and API examples.</p></main>;
}
