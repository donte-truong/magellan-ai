# Server-only values. The token is never sent to the browser.
# The Next.js backend listens on 3001 (backend/: npm run dev). MAGELLAN_API_TOKEN must equal
# the backend's RESEARCH_API_TOKEN.
MAGELLAN_API_URL=http://127.0.0.1:3001
MAGELLAN_API_TOKEN=dev-token
